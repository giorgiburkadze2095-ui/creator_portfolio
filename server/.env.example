# Copy to .env and adjust for your environment.

PORT=4000
# Frontend origin for CORS — scheme + host only, no path (never include /api
# or any other path here; that belongs in the frontend's REACT_APP_API_URL).
CLIENT_ORIGIN=http://localhost:3000
NODE_ENV=development

# Local PostgreSQL connection string.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres

# Change this in every real deployment.
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES_IN=7d

# Used once, on first boot, to create the initial SUPER_ADMIN if none exists.
SUPER_ADMIN_EMAIL=admin@example.com
SUPER_ADMIN_PASSWORD=change-this-password
SUPER_ADMIN_NAME=Site Owner
